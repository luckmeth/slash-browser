{
  "name": "slash-ad-portal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Sponsor sign-up, campaign approval and reporting for Slash's start-page tiles.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.3",
    "express": "^5.1.0"
  }
}
