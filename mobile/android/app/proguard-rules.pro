# Placeholder. Minification is off until the real shell ships; when it is turned
# on, anything reached only from JavaScript through a WebMessageListener needs
# keeping by hand, because R8 cannot see those call sites.
